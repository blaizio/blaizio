@namespace Blaizio.Ui
@*
    Styled Sheet root - a Dialog that slides in from an edge. Renders no element of its own (the
    headless BaseDialog does the open-state plumbing).
    Compose: Sheet > SheetTrigger + SheetContent > SheetHeader/SheetFooter/SheetTitle/….
*@

<BaseDialog Open="@Open"
             DefaultOpen="@DefaultOpen"
             OpenChanged="@OpenChanged"
             Modal="@Modal"
             DismissOnOutsideClick="@DismissOnOutsideClick">
    @ChildContent
</BaseDialog>

@code {
    /// <summary>Controlled open state. Leave <see langword="null"/> for uncontrolled.</summary>
    [Parameter] public bool? Open { get; set; }

    /// <summary>Initial state when uncontrolled.</summary>
    [Parameter] public bool DefaultOpen { get; set; }

    /// <summary>Raised with the new state. Enables <c>@bind-Open</c>.</summary>
    [Parameter] public EventCallback<bool> OpenChanged { get; set; }

    /// <summary>Modal (the default): traps focus, locks page scroll, renders the overlay.</summary>
    [Parameter] public bool Modal { get; set; } = true;

    /// <summary>
    /// Whether implicit dismissal (Escape, outside-click) closes it. When <see langword="false"/> the
    /// corner close button is hidden too; an explicit <c>SheetClose</c> still closes. Defaults to
    /// <see langword="true"/>.
    /// </summary>
    [Parameter] public bool DismissOnOutsideClick { get; set; } = true;

    /// <summary>The trigger and content.</summary>
    [Parameter] public RenderFragment? ChildContent { get; set; }
}
