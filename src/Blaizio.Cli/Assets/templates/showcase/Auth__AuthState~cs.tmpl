namespace {{RootNamespace}}.Auth;

/// <summary>
/// Trivial in-memory auth state for the showcase. Replace with a real
/// AuthenticationStateProvider when you wire up actual authentication.
/// </summary>
public static class AuthState
{
    public static string? UserName { get; set; }

    public static bool IsLoggedIn => UserName is not null;
}
