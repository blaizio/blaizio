@namespace Blazeo.Ui
@*
    Styled ComboboxLabel - the heading of a ComboboxGroup, over the headless BzComboboxGroupLabel. Small,
    muted text naming the group.
*@

<BzComboboxGroupLabel Class="@Tw.Merge("bz-combobox-label", Class)" Attributes="@ForwardedAttributes()">
    @ChildContent
</BzComboboxGroupLabel>

@code {
    /// <summary>The heading text.</summary>
    [Parameter] public RenderFragment? ChildContent { get; set; }

    /// <summary>Extra classes, merged over the defaults.</summary>
    [Parameter] public string? Class { get; set; }

    /// <summary>Any other attributes, forwarded to the element.</summary>
    [Parameter(CaptureUnmatchedValues = true)] public IReadOnlyDictionary<string, object>? Attributes { get; set; }

    private IReadOnlyDictionary<string, object> ForwardedAttributes() =>
        new PropBuilder().Data("slot", "combobox-label").Merge(Attributes);
}
