namespace Blazeo.Ui;

/// <summary>Size of a <see cref="Switch"/> — drives the style sheet's track/thumb scale via <c>data-size</c>.</summary>
public enum SwitchSize
{
    /// <summary>Standard.</summary>
    Default,

    /// <summary>Small.</summary>
    Sm,
}
