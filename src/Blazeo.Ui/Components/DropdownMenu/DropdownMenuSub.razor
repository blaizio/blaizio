@namespace Blazeo.Ui
@*
    Styled DropdownMenuSub - a nested submenu level. Renders no element of its own; drop a
    DropdownMenuSubTrigger and a DropdownMenuSubContent inside.
*@

<BaseDropdownMenuSub Open="@Open"
                      DefaultOpen="@DefaultOpen"
                      OpenChanged="@OpenChanged">
    @ChildContent
</BaseDropdownMenuSub>

@code {
    /// <summary>Controlled open state. Use with <c>@bind-Open</c>.</summary>
    [Parameter] public bool? Open { get; set; }

    /// <summary>Initial state when uncontrolled.</summary>
    [Parameter] public bool DefaultOpen { get; set; }

    /// <summary>Raised with the new state. Enables <c>@bind-Open</c>.</summary>
    [Parameter] public EventCallback<bool> OpenChanged { get; set; }

    /// <summary>The sub-trigger and sub-content.</summary>
    [Parameter] public RenderFragment? ChildContent { get; set; }
}
