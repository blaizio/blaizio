@namespace Blazeo.Ui
@*
    Styled DropdownMenu root - wraps the headless BaseDropdownMenu. Renders no element of its own; drop
    a DropdownMenuTrigger and a DropdownMenuContent inside. Non-modal: the page stays interactive
    while open, and Escape / outside-click / Tab / selecting an item all dismiss it.
*@

<BaseDropdownMenu Open="@Open"
                   DefaultOpen="@DefaultOpen"
                   OpenChanged="@OpenChanged">
    @ChildContent
</BaseDropdownMenu>

@code {
    /// <summary>Controlled open state. Use with <c>@bind-Open</c>.</summary>
    [Parameter] public bool? Open { get; set; }

    /// <summary>Initial state when uncontrolled.</summary>
    [Parameter] public bool DefaultOpen { get; set; }

    /// <summary>Raised with the new state. Enables <c>@bind-Open</c>.</summary>
    [Parameter] public EventCallback<bool> OpenChanged { get; set; }

    /// <summary>The trigger and content.</summary>
    [Parameter] public RenderFragment? ChildContent { get; set; }
}
