@namespace Blazeo.Ui
@*
    Styled CollapsibleTrigger. Use AsChild to put the behaviour on a styled <Button>:

        <CollapsibleTrigger>
            <AsChild Context="p"><Button Variant="ButtonVariant.Ghost" @attributes="p.Attributes">Toggle</Button></AsChild>
        </CollapsibleTrigger>
*@

<BzCollapsibleTrigger Class="@Class"
                         Attributes="@ForwardedAttributes()"
                         AsChild="@AsChild">
    @ChildContent
</BzCollapsibleTrigger>

@code {
    /// <summary>Extra classes.</summary>
    [Parameter] public string? Class { get; set; }

    /// <summary>Trigger content.</summary>
    [Parameter] public RenderFragment? ChildContent { get; set; }

    /// <summary>Render the behaviour onto your own element instead of a <c>&lt;button&gt;</c>.</summary>
    [Parameter] public RenderFragment<BzRenderProps>? AsChild { get; set; }

    /// <summary>Any other attributes, forwarded to the element.</summary>
    [Parameter(CaptureUnmatchedValues = true)] public IReadOnlyDictionary<string, object>? Attributes { get; set; }

    private IReadOnlyDictionary<string, object> ForwardedAttributes() =>
        new PropBuilder().Data("slot", "collapsible-trigger").Merge(Attributes);
}
