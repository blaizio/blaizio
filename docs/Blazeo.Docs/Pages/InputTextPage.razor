@page "/components/input-text"
<PageTitle>Input Text - Blazeo</PageTitle>

<div class="space-y-10">
    <header class="docs-page-header space-y-2.5">
        <h1 class="text-2xl font-bold tracking-tight">Input Text</h1>
        <p class="text-muted-foreground">One text field for both single- and multi-line input: it is a single-line <code class="font-mono text-sm">&lt;input&gt;</code> by default, and becomes a multi-line <code class="font-mono text-sm">&lt;textarea&gt;</code> the moment you set <code class="font-mono text-sm">Rows</code>. <code class="font-mono text-sm">@@bind-Value</code> updates live as you type.</p>
    </header>

    <UsageBlock Setup="@UsageSetup" Code="@UsageMarkup" Description="One control for both single- and multi-line text; the value updates live as you type." />

    <ExamplesHeading />

    <Demo Title="Default" Code="@DefaultCode">
        <BzInputText placeholder="Email" Type="email" Class="max-w-sm" />
    </Demo>

    <Demo Title="Multiline" Description="Set Rows to render a textarea. It still grows with its content above that height." Code="@MultilineCode">
        <BzInputText Rows="4" placeholder="Type your message here." Class="max-w-sm" />
    </Demo>

    <Demo Title="With Field" Description="Field wires the label, description, and ids; the field stays validation-agnostic." Code="@FieldCode">
        <div class="w-full max-w-sm">
            <BzField>
                <BzFieldLabel>Email</BzFieldLabel>
                <BzFieldControl Context="ctl">
                    <BzInputText @attributes="ctl" Type="email" placeholder="m@example.com" @bind-Value="_email" />
                </BzFieldControl>
                <BzFieldDescription>We'll never share it.</BzFieldDescription>
            </BzField>
        </div>
    </Demo>

    <Demo Title="Multiline with Field" Description="The same Field wiring works for the multi-line mode." Code="@MultilineFieldCode">
        <div class="w-full max-w-sm">
            <BzField>
                <BzFieldLabel>Your message</BzFieldLabel>
                <BzFieldControl Context="ctl">
                    <BzInputText @attributes="ctl" Rows="4" placeholder="Type your message here." @bind-Value="_message" />
                </BzFieldControl>
                <BzFieldDescription>It will be sent to the team.</BzFieldDescription>
            </BzField>
        </div>
    </Demo>

    <Demo Title="Invalid" Description="aria-invalid drives the destructive ring - here forced, normally set by FieldControl." Code="@InvalidCode">
        <BzInputText placeholder="Username" aria-invalid="true" Class="max-w-sm" />
    </Demo>

    <Demo Title="Disabled" Code="@DisabledCode">
        <BzInputText placeholder="Disabled" Disabled Class="max-w-sm" />
    </Demo>

    <Demo Title="File" Description="Type is single-line only (a textarea has no type), so it is ignored once Rows is set." Code="@FileCode">
        <BzInputText Type="file" Class="max-w-sm" />
    </Demo>

    <Demo Title="Live binding" Description="The label mirrors the value on every keystroke." Code="@BindingCode">
        <div class="flex w-full max-w-sm flex-col items-center gap-3">
            <BzInputText placeholder="Type something…" @bind-Value="_live" />
            <p class="text-sm text-muted-foreground">Value: <span class="font-mono">@(_live ?? "-")</span></p>
        </div>
    </Demo>

    <Demo Title="Customization"
          Description="Brand the field with unconditional border / radius and a focus ring utility - they merge over the bz-input defaults."
          Code="@CustomCode">
        <BzInputText placeholder="Search…" Class="max-w-sm rounded-xl border-emerald-600 focus-visible:border-emerald-600 focus-visible:ring-emerald-600/30" />
    </Demo>

    <Demo Title="RTL" Description="Wrapped in a DirectionProvider with Arabic copy - the labels, placeholders, and descriptions all mirror to the right." Code="@RtlCode">
        <BzDirectionProvider Direction="Direction.Rtl">
            <div class="w-full max-w-sm space-y-4">
                <BzField>
                    <BzFieldLabel>البريد الإلكتروني</BzFieldLabel>
                    <BzFieldControl Context="ctl">
                        <BzInputText @attributes="ctl" Type="email" placeholder="m@example.com" @bind-Value="_email" />
                    </BzFieldControl>
                    <BzFieldDescription>لن نشاركه مع أحد أبدًا.</BzFieldDescription>
                </BzField>
                <BzField>
                    <BzFieldLabel>رسالتك</BzFieldLabel>
                    <BzFieldControl Context="ctl">
                        <BzInputText @attributes="ctl" Rows="4" placeholder="اكتب رسالتك هنا." @bind-Value="_message" />
                    </BzFieldControl>
                    <BzFieldDescription>ستُرسل إلى الفريق.</BzFieldDescription>
                </BzField>
            </div>
        </BzDirectionProvider>
    </Demo>
</div>

@code {
    private string? _email;
    private string? _message;
    private string? _live;

    private const string UsageSetup = """
        @* _Imports.razor (or the top of the page) *@
        @using Blazeo.Ui
        """;

    private const string UsageMarkup = """
        <BzInputText placeholder="Email" Type="email" @bind-Value="_email" />

        @code { private string? _email; }
        """;

    private const string CustomCode = """
        <BzInputText placeholder="Search…" Class="max-w-sm rounded-xl border-emerald-600 focus-visible:border-emerald-600 focus-visible:ring-emerald-600/30" />
        """;

    private const string DefaultCode = """
        <BzInputText placeholder="Email" Type="email" />
        """;

    private const string MultilineCode = """
        <BzInputText Rows="4" placeholder="Type your message here." />
        """;

    private const string FieldCode = """
        <BzField>
            <BzFieldLabel>Email</BzFieldLabel>
            <BzFieldControl Context="ctl">
                <BzInputText @attributes="ctl" Type="email" placeholder="m@example.com" @bind-Value="_email" />
            </BzFieldControl>
            <BzFieldDescription>We'll never share it.</BzFieldDescription>
        </BzField>
        """;

    private const string MultilineFieldCode = """
        <BzField>
            <BzFieldLabel>Your message</BzFieldLabel>
            <BzFieldControl Context="ctl">
                <BzInputText @attributes="ctl" Rows="4" placeholder="Type your message here." @bind-Value="_message" />
            </BzFieldControl>
            <BzFieldDescription>It will be sent to the team.</BzFieldDescription>
        </BzField>
        """;

    private const string InvalidCode = """
        <BzInputText placeholder="Username" aria-invalid="true" />
        """;

    private const string DisabledCode = """
        <BzInputText placeholder="Disabled" Disabled />
        """;

    private const string FileCode = """
        <BzInputText Type="file" />
        """;

    private const string BindingCode = """
        <BzInputText placeholder="Type something…" @bind-Value="_live" />
        <p>Value: @_live</p>

        @code { private string? _live; }
        """;

    private const string RtlCode = """
        <BzDirectionProvider Direction="Direction.Rtl">
            <BzField>
                <BzFieldLabel>البريد الإلكتروني</BzFieldLabel>
                <BzFieldControl Context="ctl">
                    <BzInputText @attributes="ctl" Type="email" placeholder="m@example.com" @bind-Value="_email" />
                </BzFieldControl>
                <BzFieldDescription>لن نشاركه مع أحد أبدًا.</BzFieldDescription>
            </BzField>
            <BzField>
                <BzFieldLabel>رسالتك</BzFieldLabel>
                <BzFieldControl Context="ctl">
                    <BzInputText @attributes="ctl" Rows="4" placeholder="اكتب رسالتك هنا." @bind-Value="_message" />
                </BzFieldControl>
                <BzFieldDescription>ستُرسل إلى الفريق.</BzFieldDescription>
            </BzField>
        </BzDirectionProvider>
        """;
}
