@page "/components/combobox"
<PageTitle>Combobox - Blazeo</PageTitle>

<div class="space-y-10">
    <header class="space-y-2">
        <h1 class="text-2xl font-bold tracking-tight">Combobox</h1>
        <p class="text-muted-foreground">An autocomplete input with a popup list of suggestions. Type to filter; pick with the mouse or the arrow keys while focus stays in the input. Data-driven over <code>Items</code> with a render-prop list - single or multiple selection (with chips), groups, a clear button, and object items via <code>ItemToStringValue</code>.</p>
    </header>

    <UsageBlock Setup="@UsageSetup" Code="@UsageMarkup"
                Description="Add the using once, then bind a flat Items list and compose an input with a content list." />

    <CompositionTree Root="_anatomy"
                     Description="The full anatomy. Use ComboboxInput for the single-select field (or ComboboxChips with ComboboxValue + ComboboxChip for Multiple), and nest a ComboboxGroup with a ComboboxCollection for grouped items." />

    <ExamplesHeading />

    <Demo Title="Basic" Description="A single-select over a flat list. Typing fuzzy-filters the options; picking one fills the input and closes the popup." Code="@BasicCode">
        <BzCombobox Items="_frameworks">
            <BzComboboxInput Placeholder="Select a framework" Class="w-64" />
            <BzComboboxContent>
                <BzComboboxEmpty>No framework found.</BzComboboxEmpty>
                <BzComboboxList Context="item">
                    <BzComboboxItem Value="@item">@item</BzComboboxItem>
                </BzComboboxList>
            </BzComboboxContent>
        </BzCombobox>
    </Demo>

    <Demo Title="Multiple" Description="Set Multiple for chips. Each pick adds a token; the remove button or Backspace on an empty query removes one. The popup stays open so several can be chosen in one visit." Code="@MultipleCode">
        <BzCombobox Items="_frameworks" Multiple @bind-Values="_selectedFrameworks" AutoHighlight>
            <BzComboboxChips Class="w-80">
                <BzComboboxValue>
                    <Selection Context="values">
                        @foreach (var v in values)
                        {
                            <BzComboboxChip Value="@v">@v</BzComboboxChip>
                        }
                        <BzComboboxChipsInput Placeholder="Add framework" />
                    </Selection>
                </BzComboboxValue>
            </BzComboboxChips>
            <BzComboboxContent>
                <BzComboboxEmpty>No framework found.</BzComboboxEmpty>
                <BzComboboxList Context="item">
                    <BzComboboxItem Value="@item">@item</BzComboboxItem>
                </BzComboboxList>
            </BzComboboxContent>
        </BzCombobox>
    </Demo>

    <Demo Title="Clear button" Description="Pass ShowClear and the trailing chevron swaps for a clear button whenever a value is selected. Clearing empties the field and returns focus to the input." Code="@ClearCode">
        <BzCombobox Items="_frameworks" DefaultValue='@("ASP.NET Core")'>
            <BzComboboxInput Placeholder="Select a framework" ShowClear Class="w-64" />
            <BzComboboxContent>
                <BzComboboxEmpty>No framework found.</BzComboboxEmpty>
                <BzComboboxList Context="item">
                    <BzComboboxItem Value="@item">@item</BzComboboxItem>
                </BzComboboxList>
            </BzComboboxContent>
        </BzCombobox>
    </Demo>

    <Demo Title="Groups" Description="When the items are groups, the list render-prop yields each group; nest a ComboboxCollection for its items and a ComboboxSeparator between them. Empty groups (and their separators) drop out as you filter." Code="@GroupsCode">
        <BzCombobox Items="_timezoneGroups">
            <BzComboboxInput Placeholder="Select a timezone" Class="w-72" />
            <BzComboboxContent>
                <BzComboboxEmpty>No timezone found.</BzComboboxEmpty>
                <BzComboboxList Context="group">
                    @if (group!.Region != _timezoneGroups[0].Region)
                    {
                        <BzComboboxSeparator />
                    }
                    <BzComboboxGroup>
                        <BzComboboxLabel>@group.Region</BzComboboxLabel>
                        <BzComboboxCollection Items="@group.Zones" Context="tz">
                            <BzComboboxItem Value="@tz">@tz</BzComboboxItem>
                        </BzComboboxCollection>
                    </BzComboboxGroup>
                </BzComboboxList>
            </BzComboboxContent>
        </BzCombobox>
    </Demo>

    <Demo Title="Custom items" Description="Items can be objects. Give ItemToStringValue so the engine knows the filter + selection text, then render whatever you like inside each ComboboxItem." Code="@CustomCode">
        <BzCombobox Items="_people" ItemToStringValue="@(p => p.Name)" @bind-Value="_person">
            <BzComboboxInput Placeholder="Assign a person" Class="w-72" />
            <BzComboboxContent>
                <BzComboboxEmpty>No person found.</BzComboboxEmpty>
                <BzComboboxList Context="p">
                    <BzComboboxItem Value="@p">
                        <span class="flex flex-col">
                            <span>@p.Name</span>
                            <span class="text-muted-foreground text-xs">@p.Role</span>
                        </span>
                    </BzComboboxItem>
                </BzComboboxList>
            </BzComboboxContent>
        </BzCombobox>
    </Demo>

    <Demo Title="Auto highlight" Description="With AutoHighlight the first match stays highlighted as you type, so Enter picks it with no arrow press. Opening highlights the current selection instead (here Avalonia, the fourth option) - the highlight follows the value, never an unconditional first item." Code="@AutoHighlightCode">
        <BzCombobox Items="_frameworks" AutoHighlight DefaultValue='@("Avalonia")'>
            <BzComboboxInput Placeholder="Select a framework" Class="w-64" />
            <BzComboboxContent>
                <BzComboboxEmpty>No framework found.</BzComboboxEmpty>
                <BzComboboxList Context="item">
                    <BzComboboxItem Value="@item">@item</BzComboboxItem>
                </BzComboboxList>
            </BzComboboxContent>
        </BzCombobox>
    </Demo>

    <Demo Title="Input group" Description="Drop an InputGroupAddon inside ComboboxInput for a leading icon (or any affix) - the field still reads as one control." Code="@InputGroupCode">
        <BzCombobox Items="_timezoneGroups">
            <BzComboboxInput Placeholder="Select a timezone" Class="w-72">
                <BzInputGroupAddon>
                    <BzIcon Icon="@Icons.Outline.World" />
                </BzInputGroupAddon>
            </BzComboboxInput>
            <BzComboboxContent>
                <BzComboboxEmpty>No timezone found.</BzComboboxEmpty>
                <BzComboboxList Context="group">
                    <BzComboboxGroup>
                        <BzComboboxLabel>@group.Region</BzComboboxLabel>
                        <BzComboboxCollection Items="@group.Zones" Context="tz">
                            <BzComboboxItem Value="@tz">@tz</BzComboboxItem>
                        </BzComboboxCollection>
                    </BzComboboxGroup>
                </BzComboboxList>
            </BzComboboxContent>
        </BzCombobox>
    </Demo>

    <Demo Title="In a popup" Description="Trigger the combobox from a button by making it the anchor and moving the input inside the content. ComboboxValue shows the current pick on the button." Code="@PopupCode">
        <BzCombobox Items="_countries" ItemToStringValue="@(c => c.Label)" DefaultValue="@_countries[0]">
            <BzComboboxTrigger Anchor Class="border-input hover:bg-accent hover:text-foreground h-9 w-64 justify-between rounded-md border bg-transparent px-3 text-sm font-normal shadow-xs">
                <BzComboboxValue />
            </BzComboboxTrigger>
            <BzComboboxContent>
                <BzComboboxInput ShowTrigger="false" Anchor="false" Placeholder="Search country" />
                <BzComboboxEmpty>No country found.</BzComboboxEmpty>
                <BzComboboxList Context="c">
                    <BzComboboxItem Value="@c">@c.Label</BzComboboxItem>
                </BzComboboxList>
            </BzComboboxContent>
        </BzCombobox>
    </Demo>

    <Demo Title="Invalid" Description="Pass aria-invalid to mark the field invalid - the input group picks up the destructive ring." Code="@InvalidCode">
        <BzCombobox Items="_frameworks">
            <BzComboboxInput Placeholder="Select a framework" aria-invalid="true" Class="w-64" />
            <BzComboboxContent>
                <BzComboboxEmpty>No framework found.</BzComboboxEmpty>
                <BzComboboxList Context="item">
                    <BzComboboxItem Value="@item">@item</BzComboboxItem>
                </BzComboboxList>
            </BzComboboxContent>
        </BzCombobox>
    </Demo>

    <Demo Title="Disabled" Description="Disabled on the root greys the field and its addons and blocks interaction, while still showing the current value." Code="@DisabledCode">
        <BzCombobox Items="_frameworks" Disabled DefaultValue='@("Uno Platform")'>
            <BzComboboxInput Placeholder="Select a framework" Class="w-64" />
            <BzComboboxContent>
                <BzComboboxEmpty>No framework found.</BzComboboxEmpty>
                <BzComboboxList Context="item">
                    <BzComboboxItem Value="@item">@item</BzComboboxItem>
                </BzComboboxList>
            </BzComboboxContent>
        </BzCombobox>
    </Demo>

    <Demo Title="Customization"
          Description="Restyle any part via Class. Here the input shell gets a branded ring and radius, and the highlighted row is recolored by targeting the combobox-item slot in its data-highlighted state. Utilities win over the skin, so these take effect."
          Code="@CustomizationCode">
        <BzCombobox Items="_frameworks" AutoHighlight>
            <BzComboboxInput Placeholder="Select a framework"
                             Class="w-64 rounded-lg border-indigo-300 focus-within:ring-indigo-500/40 dark:border-indigo-800" />
            <BzComboboxContent Class="rounded-lg [&_[data-slot=combobox-item][data-highlighted]]:bg-indigo-600 [&_[data-slot=combobox-item][data-highlighted]]:text-white">
                <BzComboboxEmpty>No framework found.</BzComboboxEmpty>
                <BzComboboxList Context="item">
                    <BzComboboxItem Value="@item">@item</BzComboboxItem>
                </BzComboboxList>
            </BzComboboxContent>
        </BzCombobox>
    </Demo>

    <Demo Title="RTL" Description="Inside a right-to-left DirectionProvider the field, popup and check all mirror; the highlight and filtering behave the same." Code="@RtlCode">
        <BzDirectionProvider Direction="Direction.Rtl">
            <BzCombobox Items="_arabicFrameworks">
                <BzComboboxInput Placeholder="اختر إطار عمل" Class="w-64" />
                <BzComboboxContent>
                    <BzComboboxEmpty>لا توجد نتائج.</BzComboboxEmpty>
                    <BzComboboxList Context="item">
                        <BzComboboxItem Value="@item">@item</BzComboboxItem>
                    </BzComboboxList>
                </BzComboboxContent>
            </BzCombobox>
        </BzDirectionProvider>
    </Demo>

    <ApiReference Types="ApiFamilies.Combobox" />
</div>

@code {
    private IReadOnlyList<string> _selectedFrameworks = ["ASP.NET Core", "Blazor", "MAUI", "Avalonia"];
    private Person? _person;

    private static readonly string[] _frameworks = ["ASP.NET Core", "Blazor", "MAUI", "Avalonia", "Uno Platform"];
    private static readonly string[] _arabicFrameworks = ["إيه إس بي.نت كور", "بليزور", "ماوي", "أفالونيا", "منصة أونو"];

    private static readonly Person[] _people =
    [
        new("Jane Doe", "Engineering"),
        new("John Smith", "Design"),
        new("Alex Johnson", "Product"),
        new("Maria Garcia", "Marketing"),
    ];

    private static readonly Country[] _countries =
    [
        new("Select country"), new("Afghanistan"), new("Albania"), new("Algeria"),
        new("Argentina"), new("Australia"), new("Austria"), new("Brazil"), new("Canada"),
    ];

    private static readonly TimezoneGroup[] _timezoneGroups =
    [
        new("Americas", ["New York", "Chicago", "Denver", "Los Angeles"]),
        new("Europe", ["London", "Paris", "Berlin", "Madrid"]),
        new("Asia", ["Dubai", "Karachi", "Tokyo", "Singapore"]),
    ];

    private sealed record Person(string Name, string Role);
    private sealed record Country(string Label);
    private sealed record TimezoneGroup(string Region, string[] Zones);

    // The anatomy drawn by the Composition section - CompositionTree computes the connectors.
    private static readonly CompositionNode _anatomy = new CompositionNode("BzCombobox",
        new CompositionNode("BzComboboxInput"),
        new CompositionNode("BzComboboxChips",
            new CompositionNode("BzComboboxValue",
                new CompositionNode("BzComboboxChip",
                    new CompositionNode("BzComboboxChipRemove")),
                new CompositionNode("BzComboboxChipsInput"))),
        new CompositionNode("BzComboboxTrigger"),
        new CompositionNode("BzComboboxClear"),
        new CompositionNode("BzComboboxContent",
            new CompositionNode("BzComboboxEmpty"),
            new CompositionNode("BzComboboxList",
                new CompositionNode("BzComboboxItem"),
                new CompositionNode("BzComboboxGroup",
                    new CompositionNode("BzComboboxLabel"),
                    new CompositionNode("BzComboboxCollection")),
                new CompositionNode("BzComboboxSeparator"))));

    private const string UsageSetup = """
        @* _Imports.razor (or the top of the page) *@
        @using Blazeo.Ui
        """;

    private const string UsageMarkup = """
        <BzCombobox Items="_frameworks">
            <BzComboboxInput Placeholder="Select a framework" />
            <BzComboboxContent>
                <BzComboboxEmpty>No framework found.</BzComboboxEmpty>
                <BzComboboxList Context="item">
                    <BzComboboxItem Value="@item">@item</BzComboboxItem>
                </BzComboboxList>
            </BzComboboxContent>
        </BzCombobox>

        @code {
            private static readonly string[] _frameworks =
                ["ASP.NET Core", "Blazor", "MAUI", "Avalonia", "Uno Platform"];
        }
        """;

    private const string CustomizationCode = """
        <BzCombobox Items="_frameworks" AutoHighlight>
            <BzComboboxInput Placeholder="Select a framework"
                             Class="w-64 rounded-lg border-indigo-300 focus-within:ring-indigo-500/40 dark:border-indigo-800" />
            <BzComboboxContent Class="rounded-lg [&_[data-slot=combobox-item][data-highlighted]]:bg-indigo-600 [&_[data-slot=combobox-item][data-highlighted]]:text-white">
                <BzComboboxEmpty>No framework found.</BzComboboxEmpty>
                <BzComboboxList Context="item">
                    <BzComboboxItem Value="@item">@item</BzComboboxItem>
                </BzComboboxList>
            </BzComboboxContent>
        </BzCombobox>

        @code {
            private static readonly string[] _frameworks =
                ["ASP.NET Core", "Blazor", "MAUI", "Avalonia", "Uno Platform"];
        }
        """;

    private const string BasicCode = """
        <BzCombobox Items="_frameworks">
            <BzComboboxInput Placeholder="Select a framework" Class="w-64" />
            <BzComboboxContent>
                <BzComboboxEmpty>No framework found.</BzComboboxEmpty>
                <BzComboboxList Context="item">
                    <BzComboboxItem Value="@item">@item</BzComboboxItem>
                </BzComboboxList>
            </BzComboboxContent>
        </BzCombobox>

        @code {
            private static readonly string[] _frameworks =
                ["ASP.NET Core", "Blazor", "MAUI", "Avalonia", "Uno Platform"];
        }
        """;

    private const string MultipleCode = """
        <BzCombobox Items="_frameworks" Multiple @bind-Values="_selected" AutoHighlight>
            <BzComboboxChips Class="w-80">
                <BzComboboxValue>
                    <Selection Context="values">
                        @foreach (var v in values)
                        {
                            <BzComboboxChip Value="@v">@v</BzComboboxChip>
                        }
                        <BzComboboxChipsInput Placeholder="Add framework" />
                    </Selection>
                </BzComboboxValue>
            </BzComboboxChips>
            <BzComboboxContent>
                <BzComboboxEmpty>No framework found.</BzComboboxEmpty>
                <BzComboboxList Context="item">
                    <BzComboboxItem Value="@item">@item</BzComboboxItem>
                </BzComboboxList>
            </BzComboboxContent>
        </BzCombobox>

        @code {
            private static readonly string[] _frameworks =
                ["ASP.NET Core", "Blazor", "MAUI", "Avalonia", "Uno Platform"];
            private IReadOnlyList<string> _selected = ["ASP.NET Core", "Blazor", "MAUI", "Avalonia"];
        }
        """;

    private const string ClearCode = """
        <BzCombobox Items="_frameworks" DefaultValue='@("ASP.NET Core")'>
            <BzComboboxInput Placeholder="Select a framework" ShowClear Class="w-64" />
            <BzComboboxContent>
                <BzComboboxEmpty>No framework found.</BzComboboxEmpty>
                <BzComboboxList Context="item">
                    <BzComboboxItem Value="@item">@item</BzComboboxItem>
                </BzComboboxList>
            </BzComboboxContent>
        </BzCombobox>

        @code {
            private static readonly string[] _frameworks =
                ["ASP.NET Core", "Blazor", "MAUI", "Avalonia", "Uno Platform"];
        }
        """;

    private const string GroupsCode = """
        <BzCombobox Items="_groups">
            <BzComboboxInput Placeholder="Select a timezone" Class="w-72" />
            <BzComboboxContent>
                <BzComboboxEmpty>No timezone found.</BzComboboxEmpty>
                <BzComboboxList Context="group">
                    @if (group.Region != _groups[0].Region)
                    {
                        <BzComboboxSeparator />
                    }
                    <BzComboboxGroup>
                        <BzComboboxLabel>@group.Region</BzComboboxLabel>
                        <BzComboboxCollection Items="@group.Zones" Context="tz">
                            <BzComboboxItem Value="@tz">@tz</BzComboboxItem>
                        </BzComboboxCollection>
                    </BzComboboxGroup>
                </BzComboboxList>
            </BzComboboxContent>
        </BzCombobox>

        @code {
            private record Group(string Region, string[] Zones);
            private static readonly Group[] _groups =
            [
                new("Americas", ["New York", "Chicago", "Denver", "Los Angeles"]),
                new("Europe", ["London", "Paris", "Berlin", "Madrid"]),
            ];
        }
        """;

    private const string CustomCode = """
        <BzCombobox Items="_people" ItemToStringValue="@(p => p.Name)" @bind-Value="_person">
            <BzComboboxInput Placeholder="Assign a person" Class="w-72" />
            <BzComboboxContent>
                <BzComboboxEmpty>No person found.</BzComboboxEmpty>
                <BzComboboxList Context="p">
                    <BzComboboxItem Value="@p">
                        <span class="flex flex-col">
                            <span>@p.Name</span>
                            <span class="text-muted-foreground text-xs">@p.Role</span>
                        </span>
                    </BzComboboxItem>
                </BzComboboxList>
            </BzComboboxContent>
        </BzCombobox>

        @code {
            private record Person(string Name, string Role);
            private Person? _person;
            private static readonly Person[] _people =
                [new("Jane Doe", "Engineering"), new("John Smith", "Design")];
        }
        """;

    private const string AutoHighlightCode = """
        @* Opening highlights the selection (Avalonia); typing highlights the first match. *@
        <BzCombobox Items="_frameworks" AutoHighlight DefaultValue='@("Avalonia")'>
            <BzComboboxInput Placeholder="Select a framework" Class="w-64" />
            <BzComboboxContent>
                <BzComboboxEmpty>No framework found.</BzComboboxEmpty>
                <BzComboboxList Context="item">
                    <BzComboboxItem Value="@item">@item</BzComboboxItem>
                </BzComboboxList>
            </BzComboboxContent>
        </BzCombobox>

        @code {
            private static readonly string[] _frameworks =
                ["ASP.NET Core", "Blazor", "MAUI", "Avalonia", "Uno Platform"];
        }
        """;

    private const string InputGroupCode = """
        <BzCombobox Items="_groups">
            <BzComboboxInput Placeholder="Select a timezone" Class="w-72">
                <BzInputGroupAddon>
                    <BzIcon Icon="@Icons.Outline.World" />
                </BzInputGroupAddon>
            </BzComboboxInput>
            <BzComboboxContent>
                <BzComboboxEmpty>No timezone found.</BzComboboxEmpty>
                <BzComboboxList Context="group">
                    <BzComboboxGroup>
                        <BzComboboxLabel>@group.Region</BzComboboxLabel>
                        <BzComboboxCollection Items="@group.Zones" Context="tz">
                            <BzComboboxItem Value="@tz">@tz</BzComboboxItem>
                        </BzComboboxCollection>
                    </BzComboboxGroup>
                </BzComboboxList>
            </BzComboboxContent>
        </BzCombobox>

        @code {
            private record Group(string Region, string[] Zones);
            private static readonly Group[] _groups =
            [
                new("Americas", ["New York", "Chicago", "Denver", "Los Angeles"]),
                new("Europe", ["London", "Paris", "Berlin", "Madrid"]),
            ];
        }
        """;

    private const string PopupCode = """
        <BzCombobox Items="_countries" ItemToStringValue="@(c => c.Label)" DefaultValue="@_countries[0]">
            <BzComboboxTrigger Anchor Class="border-input hover:bg-accent hover:text-foreground h-9 w-64 justify-between rounded-md border bg-transparent px-3 text-sm font-normal shadow-xs">
                <BzComboboxValue />
            </BzComboboxTrigger>
            <BzComboboxContent>
                <BzComboboxInput ShowTrigger="false" Anchor="false" Placeholder="Search country" />
                <BzComboboxEmpty>No country found.</BzComboboxEmpty>
                <BzComboboxList Context="c">
                    <BzComboboxItem Value="@c">@c.Label</BzComboboxItem>
                </BzComboboxList>
            </BzComboboxContent>
        </BzCombobox>

        @code {
            private record Country(string Label);
            private static readonly Country[] _countries =
            [
                new("Select country"), new("Afghanistan"), new("Albania"),
                new("Algeria"), new("Argentina"), new("Australia"),
            ];
        }
        """;

    private const string InvalidCode = """
        <BzCombobox Items="_frameworks">
            <BzComboboxInput Placeholder="Select a framework" aria-invalid="true" Class="w-64" />
            <BzComboboxContent>
                <BzComboboxEmpty>No framework found.</BzComboboxEmpty>
                <BzComboboxList Context="item">
                    <BzComboboxItem Value="@item">@item</BzComboboxItem>
                </BzComboboxList>
            </BzComboboxContent>
        </BzCombobox>

        @code {
            private static readonly string[] _frameworks =
                ["ASP.NET Core", "Blazor", "MAUI", "Avalonia", "Uno Platform"];
        }
        """;

    private const string DisabledCode = """
        <BzCombobox Items="_frameworks" Disabled DefaultValue='@("Uno Platform")'>
            <BzComboboxInput Placeholder="Select a framework" Class="w-64" />
            <BzComboboxContent>
                <BzComboboxEmpty>No framework found.</BzComboboxEmpty>
                <BzComboboxList Context="item">
                    <BzComboboxItem Value="@item">@item</BzComboboxItem>
                </BzComboboxList>
            </BzComboboxContent>
        </BzCombobox>

        @code {
            private static readonly string[] _frameworks =
                ["ASP.NET Core", "Blazor", "MAUI", "Avalonia", "Uno Platform"];
        }
        """;

    private const string RtlCode = """
        <BzDirectionProvider Direction="Direction.Rtl">
            <BzCombobox Items="_arabicFrameworks">
                <BzComboboxInput Placeholder="اختر إطار عمل" Class="w-64" />
                <BzComboboxContent>
                    <BzComboboxEmpty>لا توجد نتائج.</BzComboboxEmpty>
                    <BzComboboxList Context="item">
                        <BzComboboxItem Value="@item">@item</BzComboboxItem>
                    </BzComboboxList>
                </BzComboboxContent>
            </BzCombobox>
        </BzDirectionProvider>

        @code {
            private static readonly string[] _arabicFrameworks =
                ["إيه إس بي.نت كور", "بليزور", "ماوي", "أفالونيا", "منصة أونو"];
        }
        """;
}
