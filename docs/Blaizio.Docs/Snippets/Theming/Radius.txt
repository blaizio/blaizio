/* Styles/app.css (your tokens file) - the whole scale derives from one base token. */
@theme inline {
  --radius-sm: calc(var(--radius) * 0.6);   /* rounded-sm  - badges, small chips   */
  --radius-md: calc(var(--radius) * 0.8);   /* rounded-md  - buttons, inputs       */
  --radius-lg: var(--radius);               /* rounded-lg  - cards, alerts (the base) */
  --radius-xl: calc(var(--radius) * 1.4);   /* rounded-xl  - dialogs, popovers     */
  --radius-2xl: calc(var(--radius) * 1.8);  /* rounded-2xl                         */
  --radius-3xl: calc(var(--radius) * 2.2);  /* rounded-3xl                         */
  --radius-4xl: calc(var(--radius) * 2.6);  /* rounded-4xl - pill-ish surfaces     */
}

:root {
  --radius: 0.75rem; /* the one knob: 0 = square UI, 0.5rem = subtle, 1rem = soft */
}
