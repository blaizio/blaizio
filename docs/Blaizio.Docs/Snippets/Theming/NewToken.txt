/* Your Tailwind input (Styles/app.css) - define the token in both modes, then map it. */
:root {
  --highlight: oklch(0.85 0.16 95);
  --highlight-foreground: oklch(0.25 0.06 95);
}

.dark {
  --highlight: oklch(0.45 0.12 95);
  --highlight-foreground: oklch(0.98 0.02 95);
}

@theme inline {
  --color-highlight: var(--highlight);
  --color-highlight-foreground: var(--highlight-foreground);
}

/* Now bg-highlight / text-highlight-foreground work anywhere, in both modes:
   <BzBadge Class="bg-highlight text-highlight-foreground">New</BzBadge> */
