/* Styles/app.css - your tokens file already holds every value; there is nothing to import or
   override "after". Just edit the declarations in place - every component picks the new value
   up, in both modes. */
:root {
  --primary: oklch(0.6 0.2 250);   /* brand blue instead of the default purple */
  --radius: 0.5rem;                /* tighter corners everywhere */
}

.dark {
  --primary: oklch(0.7 0.18 250);
}

/* Re-themed by hand? `blaizio contrast` audits the values against WCAG AA (light + dark). */
