/* Styles/app.css - the Tailwind input the Blaizio CLI (`blaizio init`) generated. The theme
   import is ALREADY in this file; you don't add it. Put overrides anywhere after it, so yours
   win the cascade. */
@import "./blaizio/theme.css";

/* Same tokens, your values - every component picks them up, in both modes. */
:root {
  --primary: oklch(0.6 0.2 250);   /* brand blue instead of the default purple */
  --radius: 0.5rem;                /* tighter corners everywhere */
}

.dark {
  --primary: oklch(0.7 0.18 250);
}
