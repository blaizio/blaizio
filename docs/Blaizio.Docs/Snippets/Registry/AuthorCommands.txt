# from the registry root (the folder holding Components/)
blaizio generate --name acme
#   Generated 1 item(s), 1 file(s) -> registry.json
#   Referenced but not in this tree: BzButton. If they come from the official
#   registry, add "@default/<item>" to that item's registryDependencies.

# edit registry.json: add the official items you build on, and a description
#   "registryDependencies": ["@default/utils", "@default/button"]

blaizio registry validate
blaizio build -o ./wwwroot/r
