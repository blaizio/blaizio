# what the CLI sends (all parameters optional; plain GET, same credentials as any request)
GET /r/index.json?q=button&type=registry:ui,registry:theme&limit=20&offset=40

# what a DYNAMIC registry answers: the matching page, plus pagination
{
  "name": "acme",
  "items": [
    { "name": "button", "type": "registry:ui", "description": "A button." }
  ],
  "pagination": { "total": 12, "offset": 40, "limit": 20, "hasMore": false }
}
