# default layout: <base>/<item>.json, and the catalogue at <base>/index.json
blaizio registry add @acme=https://acme.dev/r

# anywhere else: place {name} yourself
blaizio registry add @acme=https://acme.dev/api/components/{name}

# one variant per skin, chosen by the project's recorded style
blaizio registry add @acme=https://acme.dev/r/{style}/{name}.json
