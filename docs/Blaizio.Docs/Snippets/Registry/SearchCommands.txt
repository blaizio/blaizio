# query, type, and paging - the same flags against any registry
blaizio search "@acme" -q button
blaizio search "@acme" -t theme
blaizio search "@acme" -q input -t ui,lib -l 20 --offset 20

# several registries at once; the page is then a slice of the merged list
blaizio search "@acme" "@internal" -q table
