# in any Blaizio project - a local folder is a registry too
blaizio registry add "@acme=../acme/wwwroot/r"
blaizio add @acme/rating
#   + Tw.cs                         (@default/utils, skipped when already installed)
#   + Button/BzButton.razor         (@default/button, and its own dependencies)
#   + Acme/Rating/BzRating.razor    (namespace rewritten to MyApp.Components.Ui.Acme)

# then, anywhere in that project:
<BzRating @bind-Value="_stars" Max="5" />
