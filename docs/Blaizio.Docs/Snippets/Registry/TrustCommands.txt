# read every file an item would write, without writing anything
blaizio view @acme/tag

# the full plan: files, targets, NuGet packages, sibling items pulled in
blaizio add @acme/tag --dry-run

# author-side: parseable manifest, unique names, payloads present, files on disk
blaizio registry validate ./registry.json
