@* Uncontrolled - the primitive owns its state; seed it once: *@
<BaseCollapsible DefaultOpen="true">...</BaseCollapsible>

@* Controlled - you own the state (persist it, drive it from anywhere): *@
<BaseCollapsible @bind-Open="_expanded">...</BaseCollapsible>
<BzButton @onclick="() => _expanded = !_expanded">Toggle from outside</BzButton>

@code {
    private bool _expanded;
}
