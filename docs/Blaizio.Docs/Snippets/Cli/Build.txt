# Compile the manifest into the servable output: per-item JSON, per-skin inlined
# variants under r/{style}/, and the index (registry publishers):
blaizio build ./registry.json -o ./wwwroot/r --cwd ./src/My.Ui
