# Install once as a global dotnet tool; keep the tool itself current the dotnet way:
dotnet tool install -g Blaizio.Cli
dotnet tool update -g Blaizio.Cli

# (blaizio update - below - moves your PROJECT forward, not the tool.)
