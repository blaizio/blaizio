# Audit the tokens file's color pairs against WCAG AA, light AND dark.
# Failures list the pair and its ratio and exit 1 - CI-friendly:
blaizio contrast

# Show the passing pairs too / audit a different tokens file:
blaizio contrast --all
blaizio contrast --css Styles/brand.css
