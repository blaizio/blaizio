# Run the MCP server over stdio (your MCP client launches this):
blaizio mcp

# Point every tool call at a specific registry:
blaizio mcp --registry https://acme.dev/r

# Claude Code, per project:
claude mcp add blaizio -- blaizio mcp
