# Remove installed components by record: their tracked files and nothing else.
# The wiring, packages and blaizio.json stay - that's what uninstall is for.
blaizio remove color-picker            # asks first
blaizio rm colorpicker --dry-run       # short alias; preview, any casing resolves
blaizio remove slider tooltip -y       # several at once, no prompt

# A component something else still needs is refused (exit 1) - remove the
# dependents first, or override:
blaizio remove slider --force
