# Undo the wiring and adds, strictly by record - tracked components, packages,
# usings, the tokens file (only when the wiring created it), .blaizio/ and the
# host wiring. Anything you authored stays.
blaizio uninstall --dry-run     # preview first
blaizio uninstall               # asks before touching anything
blaizio un -y --json            # short alias, scriptable
