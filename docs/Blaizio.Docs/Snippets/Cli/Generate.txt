# Scan the component source tree (the folder holding Components/) into a
# registry.json manifest - dependencies inside the tree inferred, references
# outside it named so you can add "@default/<item>" by hand:
blaizio generate --name acme

# The official registry also emits its font items:
blaizio generate ./src/Blaizio.Ui --fonts
