# Which pipelines does this project already have, and what is recommended?
blaizio tailwind detect

# Wire one up (auto picks the best fit; standalone needs zero Node):
blaizio tailwind setup --mode standalone
blaizio tailwind setup --mode node -o wwwroot/css/app.css

# Pre-fetch the standalone binary into the per-user cache (sha256-verified):
blaizio tailwind fetch
blaizio tailwind fetch --tw-version v4.1.11 --force
