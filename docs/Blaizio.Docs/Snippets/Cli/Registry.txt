# Record a named registry once, then install through it:
blaizio registry add "@acme=https://acme.dev/r"
blaizio add @acme/button

# Validate a source manifest before publishing:
blaizio registry validate ./registry.json
