# Themes codes pack skin, colors, chart palette, radius, fonts and RTL into a few
# characters - new -p, add -p and apply all accept them directly:
blaizio preset decode 32r       # what does this code carry?
blaizio preset resolve          # the code that reproduces THIS project's choices
blaizio preset url 32r          # the Create-page URL for a code
blaizio preset open 32r         # ... opened in the browser
