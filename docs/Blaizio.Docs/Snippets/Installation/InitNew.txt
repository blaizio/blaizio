# A complete demo app (dashboard, forms, overlays, data, auth) in an empty folder:
mkdir MyApp && cd MyApp
blaizio init -t showcase -n MyApp

# CSS compiles as part of the build, then the app runs like any Blazor app:
dotnet run
