cd MyApp          # your existing Blazor project (Server, WASM or Web App)

# one command: wires Blaizio in (namespace, output folder, skin, RTL, Tailwind pipeline -
# prompted interactively), then pulls the components - dependencies come along automatically:
blaizio add button card dialog toast
