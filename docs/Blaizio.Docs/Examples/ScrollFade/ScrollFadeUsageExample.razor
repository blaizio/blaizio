@* One class on the scroll container. No component, no script, nothing to register. *@
<div tabindex="0" class="scroll-fade scrollbar-thin h-40 overflow-y-auto rounded-md border p-4">
    @foreach (var row in Enumerable.Range(1, 14))
    {
        <p class="py-1 text-sm">Row @row</p>
    }
</div>
