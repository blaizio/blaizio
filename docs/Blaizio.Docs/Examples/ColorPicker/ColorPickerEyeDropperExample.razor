@* The pipette opens the native EyeDropper API - pick any pixel on screen. The button renders
   only where the API exists (desktop Chromium); elsewhere the layout simply omits it. *@
<BzColorPicker @bind-Value="_color" ShowEyeDropper />

@code {
    private string? _color = "#f59e0b";
}
