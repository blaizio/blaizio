<div class="flex flex-wrap items-center justify-center gap-4">
    <BzInputDate @bind-Value="_dateDmy" Format="dd/MM/yyyy" />
    <BzInputDate @bind-Value="_dateMdy" Format="MM-dd-yyyy" />
</div>

@code {
    private DateOnly? _dateDmy = DateOnly.FromDateTime(DateTime.Today);
    private DateOnly? _dateMdy = DateOnly.FromDateTime(DateTime.Today);
}
