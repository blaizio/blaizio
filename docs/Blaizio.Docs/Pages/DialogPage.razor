@page "/docs/components/dialog"
@using Blaizio.Docs.Examples.Dialog
<PageTitle>Dialog - Blaizio</PageTitle>

<div class="space-y-10">
    <header class="docs-page-header space-y-2.5">
        <div class="flex flex-wrap items-center gap-2">
            <h1 class="text-2xl font-bold tracking-tight">Dialog</h1>
            <DocBadges Slug="dialog" />
        </div>
        <p class="text-muted-foreground">A modal window overlaid on the page: focus is trapped inside, page scroll locks, and Escape or a click on the backdrop dismisses it.</p>
    </header>

    <UsageBlock Example="typeof(DialogUsageExample)"
                Description="Add the using once, then compose a trigger and content. Wrap any action in BzDialogClose to dismiss." />

    <CompositionTree Root="Anatomy"
                     Description="The full anatomy. Header holds the title and description; wrap footer actions in BzDialogClose to close the dialog." />

    <ExamplesHeading />

    <Demo Title="Default"
          Description="The trigger and close buttons render a styled BzButton by default - no RenderAs needed. Set Variant/Size to restyle them."
          Example="typeof(DialogDefaultExample)" />

    <Demo Title="Custom trigger element (RenderAs)"
          Description="When you need the trigger to be something other than a button - a link, a card, your own component - use RenderAs and splat the behaviour onto your element."
          Example="typeof(DialogRenderAsTriggerExample)" />

    <Demo Title="Scrollable content" Example="typeof(DialogScrollableExample)" />

    <Demo Title="Sticky footer" Example="typeof(DialogStickyFooterExample)" />

    <Demo Title="Controlled" Description="The open state lives in the page - here each change raises a toast."
          Example="typeof(DialogControlledExample)" />

    <Demo Title="Nested dialogs"
          Description="A dialog can open another dialog - the inner one stacks above, and its result flows back into the outer one."
          Example="typeof(DialogNestedExample)" />

    <Demo Title="Alert dialog inside"
          Description="Guard a destructive action inside a dialog with an AlertDialog - it stacks above with its own inert backdrop; Cancel returns to the dialog, the action closes both."
          Example="typeof(DialogAlertInsideExample)" />

    <Demo Title="Without close button" Example="typeof(DialogNoCloseExample)" />
    <Demo Title="Prevent dismiss" Example="typeof(DialogPreventDismissExample)" />
    <Demo Title="Customization"
          Description="Restyle any part via Class, and the backdrop via OverlayClass - here a branded surface (custom radius, tinted border and background), an indigo title, a matching primary action, and an indigo-blurred backdrop. Root-level utilities win over the skin."
          Example="typeof(DialogCustomExample)" />
    <Demo Title="RTL" Description="Wrapped in a DirectionProvider with Arabic or Hebrew copy - the layout, labels and footer buttons mirror to the right."
          Example="typeof(DialogRtlExample)" />
</div>

@code {
    // The anatomy drawn by the Composition section - CompositionTree computes the connectors.
    private static readonly CompositionNode Anatomy = new("BzDialog",
        new CompositionNode("BzDialogTrigger"),
        new CompositionNode("BzDialogContent",
            new CompositionNode("BzDialogHeader",
                new CompositionNode("BzDialogTitle"),
                new CompositionNode("BzDialogDescription")),
            new CompositionNode("BzDialogFooter",
                new CompositionNode("BzDialogClose"))));
}
